package org.polyfrost.polyplus.client.network.p2p

import io.netty.channel.Channel
import io.netty.channel.DefaultChannelConfig

class EosP2PChannelConfig(channel: Channel) : DefaultChannelConfig(channel)
