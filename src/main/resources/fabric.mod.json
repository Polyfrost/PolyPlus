{
    "schemaVersion": 1,
    "id": "${mod_id}",
    "version": "${mod_version}",
    "name": "${mod_name}",
    "description": "${mod_description}",
    "authors": [
        "Deftu", "SubAt0mic"
    ],
    "environment": "*",
    "entrypoints": {
        "client": ["org.polyfrost.polyplus.PolyPlusEntrypoint"]
    },
    "mixins": [
        "mixins.${mod_id}.json"
    ],
    "depends": {
        "minecraft": "~${minor_mc_version}",
        "fabricloader": ">=0.15.11"
    }
}
